<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="1"
    android:versionName="1.0"
    android:installLocation="auto">

    <!-- OpenGL ES 3.1 required -->
    <uses-feature
        android:glEsVersion="0x00030001"
        android:required="true" />
    
    <!-- VR head tracking required -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true" />
    
    <!-- Hand tracking optional - allows app to run with or without controllers -->
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />
    
    <!-- Permissions -->
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <application
        android:allowBackup="false"
        android:label="RealityLib"
        android:hasCode="false">
        
        <!-- Support all Quest devices -->
        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="all" />
        
        <activity
            android:name="android.app.NativeActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:label="RealityLib"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
            android:exported="true">
            
            <!-- Tell NativeActivity the name of the .so -->
            <meta-data
                android:name="android.app.lib_name"
                android:value="realitylib" />
            
            <!-- Intent filter for VR app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- This is the correct category for OpenXR VR apps -->
                <category android:name="org.khronos.openxr.intent.category.IMMERSIVE_HMD" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
